name: Build Podman

on:
  # Manual trigger with version input
  workflow_dispatch:
    inputs:
      version:
        description: 'Podman version to build (e.g., v5.3.1)'
        required: true
        type: string
      architecture:
        description: 'Target architecture'
        required: false
        type: choice
        options:
          - amd64
          - arm64
          - both
        default: both
      variant:
        description: 'Build variant (full or minimal)'
        required: false
        type: choice
        options:
          - full
          - minimal
          - both
        default: both

  # Called by check-releases workflow
  workflow_call:
    inputs:
      version:
        description: 'Podman version to build'
        required: true
        type: string

# Ensure only one build runs at a time per version
concurrency:
  group: build-podman-${{ inputs.version }}
  cancel-in-progress: false

permissions:
  contents: write
  id-token: write  # Required for cosign OIDC

jobs:
  validate:
    name: Validate version
    runs-on: ubuntu-latest
    steps:
      - name: Validate semver pattern
        env:
          VERSION: ${{ inputs.version }}
        run: |
          # Semver pattern: v1.2.3 or v1.2 or 1.2.3 or 1.2
          SEMVER_PATTERN='^v?[0-9]+\.[0-9]+(\.[0-9]+)?$'

          if [[ ! "$VERSION" =~ $SEMVER_PATTERN ]]; then
            echo "Error: Invalid version format '$VERSION'"
            echo "Expected format: v1.2.3 or v1.2 or 1.2.3 or 1.2"
            exit 1
          fi

          echo "✓ Version '$VERSION' is valid"

  build:
    name: Build podman-${{ matrix.variant }}-${{ matrix.arch }}
    needs: validate
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        arch: [amd64, arm64]
        variant: [full, minimal]
        include:
          - arch: amd64
            runner: ubuntu-24.04
          - arch: arm64
            runner: ubuntu-24.04-arm
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install podman
        run: |
          sudo apt-get update
          sudo apt-get install -y podman

      - name: Pull container image
        run: podman pull docker.io/ubuntu:rolling

      - name: Create build directory
        run: mkdir -p build

      - name: Run build and package in container
        env:
          VERSION: ${{ inputs.version }}
          TOOL: podman
          ARCH: ${{ matrix.arch }}
          VARIANT: ${{ matrix.variant }}
        run: |
          podman run --rm \
            -v ./scripts:/workspace/scripts:ro,z \
            -v ./build:/workspace/build:rw,z \
            -e VERSION=$VERSION \
            -e TOOL=$TOOL \
            -e ARCH=$ARCH \
            -e VARIANT=$VARIANT \
            docker.io/ubuntu:rolling \
            bash -c "
              /workspace/scripts/container/setup-build-env.sh && \
              /workspace/scripts/build-tool.sh $TOOL $ARCH $VARIANT && \
              /workspace/scripts/package.sh $TOOL $VERSION $ARCH $VARIANT
            "

      - name: Verify static linking
        env:
          ARCH: ${{ matrix.arch }}
        run: |
          for binary in build/podman-$ARCH/install/bin/*; do
            if ldd "$binary" 2>&1 | grep -q "not a dynamic executable"; then
              echo "✓ $binary is statically linked"
            else
              echo "✗ $binary is NOT statically linked:"
              ldd "$binary"
              exit 1
            fi
          done

      - name: Test podman binary
        env:
          ARCH: ${{ matrix.arch }}
        run: |
          echo "Testing podman binary on native ${{ matrix.arch }} runner..."
          ./build/podman-$ARCH/install/bin/podman version
          echo "✓ podman binary works correctly"

      - name: Upload artifacts
        uses: actions/upload-artifact@v5
        with:
          name: podman-${{ matrix.variant }}-${{ matrix.arch }}
          path: |
            build/podman-${{ matrix.variant }}-linux-${{ matrix.arch }}.tar.zst
          retention-days: 7

  release:
    name: Create GitHub Release
    needs: build
    if: success()  # Only create release if all matrix builds succeeded
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Download all artifacts
        uses: actions/download-artifact@v6
        with:
          path: artifacts/

      - name: Consolidate artifacts
        run: |
          mkdir -p release/
          find artifacts/ -type f -name "*.tar.zst" -exec cp {} release/ \;
          ls -lh release/

      - name: Setup cosign
        uses: sigstore/cosign-installer@v4.0.0

      - name: Generate checksums and sign artifacts
        run: |
          chmod +x scripts/sign-release.sh
          scripts/sign-release.sh release/

      - name: Create Release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ inputs.version }}
        run: |
          TAG="podman-${VERSION}"

          # Create release notes file to avoid YAML escaping issues
          cat > /tmp/release-notes.md <<'NOTES'
          Static binaries for podman built with Clang + musl + mimalloc (containerized build)

          **ARCHITECTURES**
            - linux/amd64
            - linux/arm64

          **VARIANTS**
            - `podman-full`: Includes all runtime components (crun, conmon, fuse-overlayfs, netavark, aardvark-dns, pasta, catatonit)
            - `podman-minimal`: Binary only

          **INSTALLATION**
          ```bash
          # Download tarball for your architecture and variant
          curl -fsSL -O https://github.com/${{ github.repository }}/releases/download/podman-${{ inputs.version }}/podman-full-linux-amd64.tar.zst

          # Extract
          tar -xf podman-full-linux-amd64.tar.zst
          cd podman-${{ inputs.version }}

          # Install system-wide
          sudo cp -r usr/* /usr/
          sudo cp -r etc/* /etc/

          # Or use from current directory
          export PATH=$PWD/usr/local/bin:$PATH
          podman --version
          ```

          **VERIFICATION**

          All tarballs include SHA256 checksums and cosign signatures (keyless OIDC).

          Verify checksum:
          ```bash
          sha256sum -c checksums.txt --ignore-missing
          ```

          Verify cosign signature (example):
          ```bash
          cosign verify-blob \
            --bundle=podman-full-linux-amd64.tar.zst.bundle \
            --certificate-identity-regexp='https://github.com/.*' \
            --certificate-oidc-issuer='https://token.actions.githubusercontent.com' \
            podman-full-linux-amd64.tar.zst
          ```

          **BUILD DETAILS**
            - Built inside docker.io/ubuntu:rolling container using podman
            - Compiler: Clang with musl target + mimalloc allocator
            - Workflow ${{ github.workflow }}
            - Run ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          NOTES

          # Create or update release
          if gh release view "$TAG" &>/dev/null; then
            echo "Release $TAG already exists, uploading additional assets..."
            gh release upload "$TAG" release/* --clobber
          else
            echo "Creating new release $TAG..."
            gh release create "$TAG" \
              --title "Podman ${VERSION}" \
              --notes-file /tmp/release-notes.md \
              release/*
          fi

      - name: Release Summary
        env:
          VERSION: ${{ inputs.version }}
        run: |
          echo "### 🚀 Release Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version**: podman-${VERSION}" >> $GITHUB_STEP_SUMMARY
          echo "**Artifacts**:" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          ls -lh release/ >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Download**: https://github.com/${{ github.repository }}/releases/tag/podman-${VERSION}" >> $GITHUB_STEP_SUMMARY
